apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: myapp-tls
  namespace: "${namespace}"
spec:
  secretName: myapp-tls-secret
  dnsNames:
    - "${domain_name}"
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  