name: nxt

dns:
  parent_domain: logsr.life
  ttl: 300
  
shared:
  provider:
    name: aws
    region: us-east-1
gcp:
  project_id: cs-logscale-ps
  region: us-central1

azure:
  location: eastus
  tags:
    environment: nxt
    project: logscale-azure